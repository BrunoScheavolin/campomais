<%= form_with(model: [:admin, property], class: "row g-3", data: { turbo: false }) do |form| %>
  <div class="col-12">
    <%= form.label :name, "Nome", class: "form-label fw-bold" %>
    <%= form.text_field :name, class: "form-control", placeholder: "Ex: Fazenda Boa Vista" %>
  </div>

  <div class="col-12">
    <%= form.label :address, "Endereço", class: "form-label fw-bold" %>
    <%= form.text_field :address, class: "form-control", placeholder: "Ex: Estrada Rural, Km 12, Zona Sul" %>
  </div>

  <div class="col-12">
    <%= form.label :size, "Tamanho (alqueires)", class: "form-label fw-bold" %>
    <%= form.text_field :size, class: "form-control", placeholder: "Ex: 15" %>
  </div>

  <div class="col-12 d-flex justify-content-between mt-4">
    <a href="javascript:history.back()" class="btn btn-secondary">Voltar</a>
    <%= form.submit "Salvar", class: "btn btn-primary" %>
  </div>
<% end %>
